import React, { Component } from 'react';

// This is a React component that will hold and display all of a user's relevant profile data.
class Profile extends Component {
    state = {  }
    render() { 
        return ( <div>Hi</div> );
    }
}
 
export default Profile;