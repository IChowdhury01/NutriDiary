import React, { Component } from 'react';
import {
    Flex, 
    Spacer
  } from "@chakra-ui/react"

// React component for the website's navigation bar.

  class NavBar extends Component {
      state = {  }
      render() { 
         return ( 
         );
      }
  }

  export default NavBar;  